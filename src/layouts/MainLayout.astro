---
import BaseLayout from "./BaseLayout.astro";

export interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<BaseLayout
  title={`${title ? `${title} - ` : ""}marcialfps - Computer engineer`}
>
  <main
    class="self-center flex flex-col items-center gap-8 lg:gap-12 py-4 px-3 lg:px-0 lg:py-5 lg:w-3/5"
  >
    <slot />
  </main>
</BaseLayout>
